/**
 * @fileoverview Blog images barrel export
 * Import blog cover images and illustrations here, following the same
 * pattern as projects_images/index.ts.
 *
 * Usage:
 *   1. Add the image file to this directory
 *   2. Import it below
 *   3. Add it to the blogImages export object
 *   4. Reference it in a blog post's frontmatter coverImage field
 *      or directly from the BlogPost metadata
 *
 * Example:
 *   import mcts_cover from "./mcts_cover.webp";
 *   export const blogImages = { mcts_cover };
 */

export const blogImages = {

};
